<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taehun.totalmanager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".myBeacon"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Sign_InActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <!-- <activity android:name=".MainFragment" /> -->
        <!-- <activity android:name=".Sub1Fragment" /> -->
        <!-- <activity android:name=".Sub2Fragment" /> -->
        <activity android:name=".Board1_Activity" />

        <activity
            android:name=".Sign_UpActivity"
            android:windowSoftInputMode="adjustNothing" />

        <activity android:name=".Board1_Write_Activity" />
        <!-- <activity android:name=".Search1Fragment" /> -->
        <!-- <activity android:name=".Search2Fragment" /> -->

        <activity
            android:name=".SearchActivity"
            android:windowSoftInputMode="stateUnchanged"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".Board_Comment_Activity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".Board1_Search_Activity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity android:name=".Popup2Activity" android:theme="@style/Theme.AppCompat.Dialog"/>
        <activity android:name=".PopupActivity" android:theme="@style/Theme.AppCompat.Dialog"/>
        <activity
            android:name=".BeaconMap.BeacomMapActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".BeaconMap.Beacon1Fragment" />
        <activity android:name=".BeaconMap.Beacon2Fragment" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".BeaconScan.CustomDialog2"></activity>
    </application>

</manifest>